<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:padding="16dp"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:id="@+id/layoutFlags"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- TR Emoji Flag -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFlagTR"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:minWidth="52dp"
            android:minHeight="52dp"
            android:text="🇹🇷"
            android:textSize="22sp"
            android:textColor="@android:color/black"
            android:contentDescription="@string/flag_tr"
            android:backgroundTint="@android:color/white"
            app:cornerRadius="26dp"
            app:strokeWidth="1dp"
            app:strokeColor="@android:color/black"
            app:iconPadding="0dp" />

        <View
            android:layout_width="16dp"
            android:layout_height="0dp" />

        <!-- EN Emoji Flag (GB) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFlagEN"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:minWidth="52dp"
            android:minHeight="52dp"
            android:text="🇬🇧"
            android:textSize="22sp"
            android:textColor="@android:color/black"
            android:contentDescription="@string/flag_en"
            android:backgroundTint="@android:color/white"
            app:cornerRadius="26dp"
            app:strokeWidth="1dp"
            app:strokeColor="@android:color/black"
            app:iconPadding="0dp" />
    </LinearLayout>

    <ScrollView
        android:id="@+id/scrollManual"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/layoutFlags"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/txtManualContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#000000"
            android:textSize="16sp"
            android:lineSpacingExtra="4dp" />
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>
